#!/bin/bash
IP_ADDR="23.185.0.4" 
./change-rec.sh greatplains ${IP_ADDR}
# ./change-rec.sh rmacc ${IP_ADDR}
./change-rec.sh kycyberteam ${IP_ADDR}
./change-rec.sh careers-ct ${IP_ADDR}
# ./change-rec.sh sweeter ${IP_ADDR}
# ./change-rec.sh mines ${IP_ADDR}
# ./change-rec.sh trecis ${IP_ADDR}
./change-rec.sh campuschampions ${IP_ADDR}
./change-rec.sh coco ${IP_ADDR}
./change-rec.sh usrse ${IP_ADDR}
./change-rec.sh connect ${IP_ADDR}
